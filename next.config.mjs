/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['fakestoreapi.com', `filtros.digitaltelecom.online/api/products`],
      },
};

export default nextConfig;
